#ifndef CONDITION_H
#define CONDITION_H
#include "struct.h"
#include <stdio.h>
#include <ctype.h>
#include <string.h>

int checkFamily(Student* students);
int calculateProgramStatus();
int readValue(int* count);
int checkSizeCondition(int count);

#endif // CONDITION_H
